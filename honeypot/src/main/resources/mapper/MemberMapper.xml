<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.cloud.honeypot.mapper.MemberMapper">

    <!-- 이메일 중복 체크 -->
    <select id="countByEmail" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM member WHERE email = #{email}
    </select>

    <!-- 회원가입 -->
    <insert id="insertMember" parameterType="org.cloud.honeypot.dto.MemberDto">
        INSERT INTO member (email, password, name, role, created_at)
        VALUES (#{email}, #{password}, #{name}, 'USER', NOW())
    </insert>

    <!-- 로그인 (이메일로 조회) -->
    <select id="selectMemberByEmail" parameterType="String"
            resultType="org.cloud.honeypot.dto.MemberDto">
        SELECT member_id, email, password, name, role, created_at
        FROM member
        WHERE email = #{email}
    </select>

    <!-- 회원 정보 조회 (ID로) -->
    <select id="selectMemberById" parameterType="Long"
            resultType="org.cloud.honeypot.dto.MemberDto">
        SELECT member_id, email, password, name, role, created_at
        FROM member
        WHERE member_id = #{memberId}
    </select>

    <!-- 회원 정보 수정 -->
    <update id="updateMember" parameterType="org.cloud.honeypot.dto.MemberDto">
        UPDATE member
        SET name = #{name},
            password = #{password}
        WHERE member_id = #{memberId}
    </update>

    <!-- 회원 탈퇴 -->
    <delete id="deleteMember" parameterType="Long">
        DELETE FROM member WHERE member_id = #{memberId}
    </delete>

</mapper>